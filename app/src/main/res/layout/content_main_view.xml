<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".activitys.MainViewActivity"
    tools:showIn="@layout/app_bar_main_view">

    <include layout="@layout/fragment_popular_series"/>

    <ProgressBar
        android:id="@+id/progressSeriesService"
        android:indeterminateTint="@color/colorStatusBar"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="center"
        android:visibility="invisible"/>
</FrameLayout>